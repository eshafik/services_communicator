============
Installation
============

At the command line::

    $ easy_install services_communicator

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv services_communicator
    $ pip install services_communicator
